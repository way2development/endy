import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs'
import { ProductReviews } from './ProductReviews'

export const Template = (args) => (
  <div style={{ width: '414px', height: '100px' }}>
    <ProductReviews {...args} />
  </div>
)

<Meta title='Components/Product Reviews' component={ProductReviews} />

# Product Reviews

## Usage

This component is used across all product pages. It appears directly below the product name and links further down the page to the reviews section of the layout.

<Canvas>
  <Story
    name='Product Reviews'
    args={{
      rating: 4.6778,
      totalReviews: 13924,
      locale: 'en',
      isLoading: false
    }}
  >
    {Template.bind()}
  </Story>
</Canvas>

### ProductReviews Props

<ArgsTable of={ProductReviews} />
