import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs'
import { NewFlag } from './NewFlag'

export const Template = (args) => <NewFlag {...args} />

<Meta title='Components/New Flag' component={NewFlag} />

# New Flag

This component creates a new flag banner. Its purpose is to highlight new product releases on the Shop Module or Navigation.

## Default

<Canvas>
  <Story name='Default' args={{ locale: 'en', isNewProduct: true }}>
    {Template.bind()}
  </Story>
</Canvas>

## Props

<ArgsTable of={NewFlag} />
